const myName = "Carlos";

if (myName === "Carlos") {
  console.log("Hello World!");
}

let isLoggedIn = false;

if (isLoggedIn) {
  console.log("User is NOT logged in");
}

const enteredUsername = "Carlos"; //If the string value is EMPTY, JavaScipt will read it like a boolean value (false), if it is not empty (true)

if (enteredUsername) {
  console.log("Input is valid!");
}
